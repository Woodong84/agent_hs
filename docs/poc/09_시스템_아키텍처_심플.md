# 시스템 아키텍처 다이어그램 (심플 버전)

## 전체 구조

```mermaid
flowchart TD
    USER["👤 사용자<br/>품목명 · 재질 · 용도 · 수출입"]

    GATEWAY["🌐 Render Cloud Gateway<br/>(HTTPS · Python 3.11)"]

    UI["🖥️ Streamlit Web UI"]

    AGENT["🤖 Agent Orchestrator<br/>LangChain ReAct AgentExecutor"]

    LLM["🧠 Azure OpenAI<br/>GPT-4.1"]

    subgraph RAG["📚 RAG Pipeline"]
        EMB["🔢 text-embedding-3-small<br/>(1,536 dim)"]
        DB["🗄️ Pinecone Serverless<br/>Vector DB<br/>(cosine · 246문서 / 6K벡터)"]
    end

    T1["🔍 search_hs_code_rag<br/>(RAG 검색 + 충돌 감지)"]
    T2["💱 query_tax_rate<br/>(세율 조회)"]
    T3["📋 save_audit_log<br/>(감사 로그)"]

    TAX["📊 정적 세율 테이블<br/>(28·38·39·69·76·84·85·90류)"]
    LOG["📝 audit_log.json<br/>(JSONL 누적)"]

    INGEST["📥 Ingest Pipeline<br/>RecursiveTextSplitter<br/>+ Pinecone Upsert"]
    DOCS["📄 원본 문서<br/>(HS 해석지침 PDF · 품목 TXT)"]

    RESULT["📊 결과 화면<br/>Top-3 · 근거 · 세율 · 경고"]

    %% 메인 데이터 흐름
    USER -->|"① HTTPS 요청"| GATEWAY
    GATEWAY --> UI
    UI -->|"② 품목 정보 전달"| AGENT
    AGENT <-->|"③ Thought / Action / Final"| LLM
    AGENT -->|"④ Tool 호출"| T1
    AGENT -->|"⑥ Tool 호출"| T2
    AGENT -->|"⑦ 직접 호출"| T3

    %% RAG Pipeline 내부
    T1 -->|"쿼리 임베딩"| EMB
    EMB -->|"1536dim 벡터"| DB
    DB -->|"⑤ Top-5 유사 청크"| T1

    %% 세율 / 로그
    T2 --> TAX
    TAX --> T2
    T3 --> LOG

    %% 결과 반환
    AGENT -->|"⑧ Final Answer"| RESULT
    RESULT --> UI
    UI --> USER

    %% 적재 (별도 경로)
    DOCS -.->|"청킹"| INGEST
    INGEST -.->|"임베딩 후 upsert"| DB

    classDef user fill:#dbeafe,stroke:#2563eb,stroke-width:2px,color:#1e3a5f
    classDef gateway fill:#e0e7ff,stroke:#4f46e5,stroke-width:2px,color:#1e1b4b
    classDef ui fill:#eff6ff,stroke:#3b82f6,stroke-width:2px,color:#1e3a5f
    classDef agent fill:#1e3a5f,stroke:#2563eb,stroke-width:2px,color:#ffffff
    classDef llm fill:#fff7ed,stroke:#f97316,stroke-width:2px,color:#7c2d12
    classDef tool fill:#f0f9ff,stroke:#0284c7,stroke-width:1.5px,color:#0c4a6e
    classDef data fill:#f0fdf4,stroke:#16a34a,stroke-width:1.5px,color:#14532d
    classDef ingest fill:#faf5ff,stroke:#9333ea,stroke-width:1.5px,color:#581c87

    class USER user
    class GATEWAY gateway
    class UI,RESULT ui
    class AGENT agent
    class LLM llm
    class T1,T2,T3 tool
    class DB,EMB,TAX,LOG data
    class INGEST,DOCS ingest
```

---

## 한눈에 보는 데이터 흐름

```
사용자
   ↓ HTTPS
Render Cloud Gateway
   ↓
Streamlit Web UI
   ↓
Agent Orchestrator (LangChain ReAct)
   ↕ Azure OpenAI GPT-4.1  (Thought/Action 추론)
   │
   ├─→ search_hs_code_rag
   │     └─→ text-embedding-3-small → Pinecone Vector DB
   │
   ├─→ query_tax_rate
   │     └─→ 정적 세율 테이블 (9개 류)
   │
   └─→ save_audit_log
         └─→ audit_log.json (JSONL)
   ↓
결과 화면 (Top-3 + 근거 + 세율 + 경고)
```

---

## 핵심 컴포넌트 (실제 기술명)

| 컴포넌트 | 기술 스택 | 역할 |
|---------|----------|------|
| **Gateway** | Render Cloud (Python 3.11) | HTTPS 진입점 · 클라우드 호스팅 |
| **UI** | Streamlit | 사용자 입력 / 결과 시각화 |
| **Agent Orchestrator** | LangChain ReAct AgentExecutor | 도구 선택·호출 순서 결정 |
| **LLM** | Azure OpenAI **GPT-4.1** | 자연어 추론 (Thought→Action→Final) |
| **Embedding Model** | Azure **text-embedding-3-small** (1536 dim) | 쿼리·문서 벡터화 |
| **Vector DB** | **Pinecone Serverless** (cosine) | 246문서 / 약 6,000벡터 |
| **RAG Pipeline** | search_hs_code_rag + Pinecone + Embedding | 유사도 검색 + Top-3 + 충돌 감지 |
| **Tax Service** | query_tax_rate + 정적 세율 테이블 | 9개 류 기본/FTA 세율 |
| **Audit Log** | save_audit_log + JSONL 파일 | 감사 추적 자동 기록 |
| **Ingest Pipeline** | RecursiveTextSplitter + Pinecone Upsert | PDF/TXT → 청킹 → 임베딩 → 적재 |
