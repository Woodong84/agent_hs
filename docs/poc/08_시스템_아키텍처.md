# 시스템 아키텍처 다이어그램

## 전체 시스템 아키텍처

```mermaid
flowchart TD
    %% ────────────────────────────────────────
    %% 사용자 레이어
    %% ────────────────────────────────────────
    subgraph USER["👤 사용자 레이어"]
        direction TB
        UI_INPUT["📝 입력 패널\n─────────────────\n• 품목명 (product_name)\n• 재질 (material)\n• 용도 (purpose)\n• 수출입 방향\n• 기존 HS-Code (선택)\n• 유사도 임계값 슬라이더\n  (기본값 0.45)"]
        UI_OUTPUT["📊 결과 패널\n─────────────────\n• HS-Code Top-3 후보\n• 신뢰도 (높음/중간/낮음)\n• 근거 문서 + 인용 문장\n• 세율 정보 (기본/FTA)\n• 충돌 경고 배너\n• A/B 비교 카드\n• 면책 문구 (고정 출력)"]
    end

    %% ────────────────────────────────────────
    %% 클라우드 애플리케이션 레이어
    %% ────────────────────────────────────────
    subgraph RENDER["☁️ Render Cloud  |  Python 3.11  |  Streamlit"]
        direction TB

        subgraph ORCHESTRATOR["🤖 Orchestrator Agent  ―  agents/orchestrator.py"]
            direction TB
            REACT["⚙️ ReAct AgentExecutor\n(LangChain)\n─────────────────\n• max_iterations: 5\n• return_intermediate_steps: True\n• handle_parsing_errors: True\n• verbose: True"]
            LLM_PRIMARY["🟦 Azure OpenAI gpt-4.1\n─────────────────\n• max_tokens: 4,096\n• API Version: 2024-12-01-preview\n• Role: 메인 추론 LLM\n  (Thought → Action → Observation)"]
            LLM_FALLBACK["🟪 Claude Sonnet 4.6\n(Anthropic)\n─────────────────\n• Role: Azure 미설정 시\n  자동 Fallback LLM"]
            REACT -->|"use_azure=True"| LLM_PRIMARY
            REACT -->|"use_azure=False"| LLM_FALLBACK
        end

        subgraph TOOLS["🔧 Tool Layer  ―  tools/"]
            direction TB
            T1["🔍 search_hs_code_rag\n─────────────────\n• 입력: product_name × 2 (가중치)\n       + material + purpose\n       + trade_direction\n• 처리: 쿼리 임베딩 → Pinecone 검색\n• 출력: Top-3 후보 + 근거 + 충돌 정보\n• Fallback 판정: 유사도 < threshold\n• 충돌 감지:\n  TYPE-1: 후보 간 류(類) 분리\n  TYPE-2: 기존코드 ↔ 추천코드 불일치\n  TYPE-3: 근거 문서 간 상충"]
            T2["💱 query_tax_rate\n─────────────────\n• 입력: hs_code, trade_direction\n• 처리: 앞 2자리(류)로 테이블 조회\n• 출력: 기본관세율\n       한-미/EU/중/ASEAN FTA 협정세율\n• 커버: 28·38·39·69·76·84·85·90류"]
            T3["📋 save_audit_log\n─────────────────\n• 호출 방식: 직접 invoke() ← INVARIANT-01\n  (LLM 경유 금지 — input 빈 {} 버그 방지)\n• 저장 내용:\n  입력값 / RAG결과 / 세율 / 후보\n  Fallback여부 / 충돌정보 / 응답시간\n• 형식: JSONL 누적 append-only"]
        end

        subgraph SETTINGS["⚙️ 설정 레이어  ―  configs/"]
            direction LR
            CFG["settings.py\nPydantic-Settings\n환경변수 관리"]
            PROMPT["prompts.py\n시스템 프롬프트\n면책 문구 (INVARIANT-07)"]
        end
    end

    %% ────────────────────────────────────────
    %% 외부 서비스 레이어
    %% ────────────────────────────────────────
    subgraph EXTERNAL["🌐 외부 서비스 레이어"]
        direction LR

        subgraph PINECONE["Pinecone Serverless"]
            P_INDEX["📦 인덱스: hs-code-docs\n─────────────────\n• 차원(dim): 1,536\n• 메트릭: cosine\n• 벡터 수: ~6,000개\n• 문서 수: 246개\n  (HS 해석지침 232 + 품목 TXT 14)"]
        end

        subgraph AZURE_OAI["Azure OpenAI"]
            EMBED_MODEL["🔢 text-embedding-3-small\n─────────────────\n• 출력 차원: 1,536\n• 역할: 쿼리/문서 벡터화"]
            GPT_MODEL["🧠 gpt-4.1\n─────────────────\n• 역할: ReAct 추론\n  Thought / Action / Final Answer"]
        end
    end

    %% ────────────────────────────────────────
    %% 스토리지 레이어
    %% ────────────────────────────────────────
    subgraph STORAGE["💾 로컬 스토리지"]
        TAX_TABLE["📊 정적 세율 테이블\ntools/query_tax.py 내장\n─────────────────\n28류: 무기화학품\n38류: 기타화학품\n39류: 플라스틱\n69류: 세라믹\n76류: 알루미늄\n84류: 일반기계\n85류: 전기기기\n90류: 광학기기"]
        AUDIT_FILE["📋 logs/audit_log.json\nJSONL 누적 저장\n─────────────────\n• 입력 데이터\n• RAG 검색 결과\n• 세율 조회 결과\n• 담당자 최종 선택\n• 수정 사유\n• 응답 시간(초)"]
    end

    %% ────────────────────────────────────────
    %% 데이터 적재 파이프라인
    %% ────────────────────────────────────────
    subgraph INGEST["📥 데이터 적재 파이프라인  ―  data_ingest/ingest.py"]
        direction LR
        RAW_DOCS["📄 원본 문서\ndata/raw/\n─────────\nPDF: HS 품목분류 사례집\nTXT: 품목별 지식 문서"]
        SPLITTER["✂️ RecursiveTextSplitter\n─────────────────\nchunk_size: 500\nchunk_overlap: 50\nchunk_id 메타데이터 부여"]
        UPLOADER["⬆️ Pinecone Upsert\n─────────────────\nbatch_size: 50\n(무료 플랜 rate limit)\n적재 전 delete_all=True\n(중복 누적 방지)"]
        RAW_DOCS --> SPLITTER --> UPLOADER
    end

    %% ────────────────────────────────────────
    %% 데이터 흐름 화살표
    %% ────────────────────────────────────────

    %% 사용자 ↔ 앱
    UI_INPUT -->|"① 품목 정보 전송"| REACT
    REACT -->|"⑧ 최종 추천 결과 반환"| UI_OUTPUT

    %% Orchestrator ↔ Tools
    REACT -->|"② Tool 선택 및 호출\n(ReAct Thought→Action)"| T1
    REACT -->|"④ Tool 호출"| T2
    REACT -->|"⑥ 직접 호출 (LLM 경유 X)"| T3

    %% Tools ↔ 외부 서비스
    T1 -->|"쿼리 임베딩 요청"| EMBED_MODEL
    EMBED_MODEL -->|"1536dim 벡터 반환"| T1
    T1 -->|"③ 벡터 유사도 검색\n(top-k=5, cosine)"| P_INDEX
    P_INDEX -->|"③ 문서 청크 + 유사도 점수 반환"| T1

    %% Tools ↔ 스토리지
    T2 -->|"⑤ 류 코드(2자리) 조회"| TAX_TABLE
    TAX_TABLE -->|"⑤ 세율 정보 반환"| T2
    T3 -->|"⑦ JSONL append"| AUDIT_FILE

    %% Tool 결과 → Orchestrator
    T1 -->|"후보 Top-3 + conflict 정보"| REACT
    T2 -->|"기본세율 + FTA 협정세율"| REACT

    %% LLM 추론
    LLM_PRIMARY -->|"Final Answer 생성"| REACT

    %% 적재 파이프라인 → Pinecone
    UPLOADER -->|"벡터 upsert"| P_INDEX
    EMBED_MODEL -.->|"임베딩 생성 (적재 시)"| UPLOADER

    %% 설정 참조
    CFG -.->|"환경변수 주입"| REACT
    PROMPT -.->|"시스템 프롬프트 주입"| REACT

    %% ────────────────────────────────────────
    %% 스타일
    %% ────────────────────────────────────────
    classDef userStyle fill:#dbeafe,stroke:#2563eb,stroke-width:2px,color:#1e3a5f
    classDef agentStyle fill:#1e3a5f,stroke:#2563eb,stroke-width:2px,color:#ffffff
    classDef toolStyle fill:#eff6ff,stroke:#3b82f6,stroke-width:1.5px,color:#1e3a5f
    classDef externalStyle fill:#fff7ed,stroke:#f97316,stroke-width:2px,color:#7c2d12
    classDef storageStyle fill:#f0fdf4,stroke:#16a34a,stroke-width:1.5px,color:#14532d
    classDef ingestStyle fill:#faf5ff,stroke:#9333ea,stroke-width:1.5px,color:#581c87

    class UI_INPUT,UI_OUTPUT userStyle
    class REACT,LLM_PRIMARY,LLM_FALLBACK agentStyle
    class T1,T2,T3 toolStyle
    class P_INDEX,EMBED_MODEL,GPT_MODEL externalStyle
    class TAX_TABLE,AUDIT_FILE storageStyle
    class RAW_DOCS,SPLITTER,UPLOADER ingestStyle
```

---

## 레이어별 요약

| 레이어 | 구성 요소 | 역할 |
|--------|----------|------|
| **사용자** | Streamlit Web UI | 입력 수집 / 결과 시각화 |
| **Orchestrator** | ReAct AgentExecutor (LangChain) | 도구 호출 순서 결정 · 추론 |
| **LLM** | Azure OpenAI gpt-4.1 / Claude Sonnet 4.6 | 자연어 추론 · 응답 생성 |
| **Tool: RAG** | search_hs_code_rag | Pinecone 검색 + 충돌 감지 |
| **Tool: 세율** | query_tax_rate | 정적 세율 테이블 조회 |
| **Tool: 로그** | save_audit_log | 감사 로그 직접 저장 |
| **벡터 DB** | Pinecone Serverless (1536dim, cosine) | 문서 유사도 검색 |
| **임베딩** | text-embedding-3-small | 쿼리·문서 벡터화 |
| **스토리지** | audit_log.json / 정적 세율 테이블 | 로컬 영구 저장 |
| **배포** | Render Cloud (Python 3.11) | 클라우드 서비스 운영 |

## 핵심 데이터 흐름 (8단계)

```
① 사용자 입력 → Streamlit UI
② Orchestrator ReAct 루프 시작 (Thought → Action)
③ search_hs_code_rag: 쿼리 임베딩 → Pinecone 코사인 검색 → Top-3 + 충돌 감지
④ query_tax_rate: HS 앞 2자리(류)로 세율 테이블 조회
⑤ 세율 정보 반환
⑥ save_audit_log: 직접 invoke() (LLM 미경유)
⑦ JSONL 감사 로그 저장
⑧ Final Answer 생성 → Streamlit 결과 패널 출력
```
